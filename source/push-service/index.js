// eslint-disable-next-line max-params
(function main(Push_service) {
    return module.exports = Object.freeze({
        Push_service,
        }); // eslint-disable-line indent
}(
    require('./push-service'),
));
